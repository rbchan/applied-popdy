---
format:
  revealjs:
#    theme: [default, custom.scss]
    theme: default
    pagetitle: "The BIDE model"
    slide-number: true
    toc: false
    margin: 0.1
    fig-align: "center"
    background-color: "gray"
    auto-stretch: false
    css: custom.css
lang: en
---

## {content-align="center" style="font-size: 110%;"}

<center>
Applied Population Dynamics\
Lab 1 -- Excel and R Basics
</center>

![](figs/excel.png){.framed-img fig-align="center" width="80%"}

# Referencing {visibility="hidden"}


## Referencing -- Column B

![](figs/columnB.png)


## Referencing -- Row 3

![](figs/row3.png)


## Referencing -- Cell B3

![](figs/cellB3.png)


## Create Sequence Using Auto-fill

![](figs/sequence.png)


To use auto-fill: begin a sequence, highlight the cells, and then drag
the box at the bottom-right of the last cell.\


## Relative Cell References

![](figs/relative-ref.png)

![](figs/relative-ref2.png)


## Relative Cell References

![](figs/relative-ref3.png)

::: center
Values of reference will change when using auto-fill
:::


## Absolute Cell References

![](figs/absolute-ref.png)


## Absolute Cell References

![](figs/absolute-ref2.png)

::: center
Dollar sign "locks" a reference so that auto-fill won't change it
:::


## Partial Cell References

![](figs/partial-ref.png)


# Equations

![](figs/equation.png)



## Equations

![](figs/equation2.png)

Equations also have to begin with the equals sign (` =`) 


## Equations

![](figs/equation3.png)


## Equations

![](figs/equation4.png)




## Formulas

![](figs/formula.png)


# Graphics

## Graphics

![](figs/scatterlines.png)



## Graphics

![](figs/scatterlines2.png)


## Graphics

![](figs/scatterlines3.png)


::: center
Add a line for males
:::



## Customize

![](figs/customize.png)

::: center
Add legend
:::


## Customize

![](figs/customize2.png)

::: center
Add axis labels
:::


## Customize

![](figs/customize3.png)

::: center
Change line color
:::


# R

## R -- Software for statistical computing

**R** can be downloaded [here](https://www.r-project.org): <https://www.r-project.org/>\

You can use the graphical user interface that comes with **R**, or you
can run **R** through a system like **ESS+emacs** or **RStudio**
(<https://posit.co/download/rstudio-desktop/>)\

Most people use **RStudio** these days


## RStudio

![](figs/rstudio-no-script.png)


## RStudio

To create a new script, click: `File > New File > R Script`

![](figs/rstudio-open-script.png)

Save your script using: `File > Save As`


## RStudio

![](figs/rstudio-script-graphics.png)


## Reproducing the Excel exercise



Create an object called [`year`]{style="background-color: inlinecolor"}
to hold the sequence of years.\

```{r}
#| echo: true
#| label: year
year <- 1950:1961 # A vector of integers
year              # Type the name of an object to see its values
```



Note that you can use [`seq`]{style="background-color: inlinecolor"} to
create sequences in a more general way. The
[`c`]{style="background-color: inlinecolor"} function combines two or
more vectors.

```{r}
#| echo: true
#| label: seq
updown <- c(seq(from=20, to=100, by=20), seq(from=100, to=20, by=-20))
updown            # Two sequences, combined
```   


Use the `length` function to determine the number of values in a
vector.

```{r}
#| echo: true
#| label: nYears
nYears <- length(year)
nYears
```




## A simple population model

Create an empty vector to store the data on females. Set female
abundance to 100 in the first year.

```{r}
#| echo: true
#| label: females
females <- rep(NA, nYears)
females[] <- 100
```

Use a "for loop" to compute female abundance in subsequent years.
```{r}
#| echo: true
#| label: femaleLoop
for(t in 2:nYears) {
    females[] <- females[] + females[]*0.01
}
```

[**We will use "for loops" for almost every population model that we
implement in R**]{.alert}\



## A simple population model

::: {.columns}
::: {.column width='50%'}
Generate the data on males using a single line of code.
```{r}
#| echo: true
#| label: males
males <- females*0.8
```
:::

::: {.column width='50%'}
Put the objects in a `data.frame`

```{r}
#| echo: true
#| label: data
model1 <- data.frame(year, females, males)
model1
```
:::
:::


## Graphics

```{r}
#| echo: true
#| label: plot
#| fig-width: 7
#| fig-height: 4.6
plot(females ~ year, data=model1, type="o", xlab="Year", ylab="Abundance",
     lwd=2, pch=16, ylim=c(0, 120))
lines(males ~ year, data=model1, type="o", col="blue", lwd=2, pch=16)
legend(x=1950, y=40, legend=c("Females", "Males"), col=c("black", "blue"), lty=1, pch=16)
```

## Assignment

1.  Create an Excel file and name it "Yourlastname_Yourfirstname".

2.  Create the sheet shown on the next page using the techniques covered
    in this lab.

    - Use auto-fill to create the first two columns.

    - For the "Adults" column, use the equation shown for cells C3
      through C22. Note: For cell C2, you can directly enter the value
      "10".

3.  Copy "Sheet1" to a new sheet and change the color and thickness of
    the lines. You can pick any colors and thicknesses you want.

4.  Replicate steps 1--3 using a "for loop" in a self-contained R
    script. Note that you will need code to create both graphs---the
    original one and the one with the new line color and thickness.

    - Hint: You can make the juveniles data using
      [`seq`]{style="background-color: inlinecolor"} and
      [`c`]{style="background-color: inlinecolor"}, like we did with the
      [`updown`]{style="background-color: inlinecolor"} example earlier.

5.  Upload the Excel workbook (with both sheets) and the R script to ELC
    by 5:00pm on Friday.


